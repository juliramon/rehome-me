<section id="sittersList">
  <div class="header">
    <h1>List of sitters</h1>
  </div>
  <div class="animals wrapper">
    {{#each sitters}}
      {{> userCard this}}
      {{else}}
      <p>There are no sitters yet</p>
    {{/each}}
  </div>
  {{> addButton}}
</section>